import 'package:flutter/material.dart';

class EarthCoreDistance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("Calculate Distance to the Earth's Core", textDirection: TextDirection.ltr);
  }
}